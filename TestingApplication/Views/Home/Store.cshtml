@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
<link rel="stylesheet" href="~/css/StyleSheet.css" type="text/css" />

@model TestingApplication.Models.Home.StoreViewModel



    <div id="alert" class="alert alert-warning" role="alert">
        BE SURE TO LOG QUANTITY BEFORE ADDING TO CART. LOG ONE ITEM QUANTITY AT A TIME.
    </div>
<br/>


    <div id="table">

        <table>
            <tr>
                <th>Item</th>
                <th>Price</th>
                <th>Quantity</th>
                @*<th></th>*@

            </tr>
            @{
                foreach (var item in Model.TheStore)
                {
                    <tr>
                        

                        <td>@item.Name</td>
                        <td>@item.Price</td>

                        <td>
                            @*<select asp-for="@item.Quantity">
                                @{
                                    for (int i = 1; i < 21; i++)
                                    {
                                        <option>@i</option>
                                    }
                                }

                            </select>*@
                            
                            <form>
                                <input asp-for="Quantity" />
                                <br />
                                <button type="submit" class="btn-dark" asp-action="AddToCart" asp-route-name="@item.Name" asp-route-qty="@Model.Quantity">Add to Cart</button>
                            </form>
                        </td>

                        @*<td><button type="submit" class="btn-dark" asp-action="AddToCart" asp-route-name="@item.Name" asp-route-qty="@Model.Quantity">Add to Cart</button></td>*@
                        @*<td><a class="text-info" asp-action="AddToCart" asp-route-name="@item.Name" asp-route-qty="@Model.Quantity">Add to Cart</a></td>*@
                    </tr>
                }
            }


        </table>
    </div>
